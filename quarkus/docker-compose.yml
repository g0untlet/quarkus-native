services:
  quarkus-native:
    image: ghcr.io/g0untlet/quarkus-native:latest
    container_name: quarkus-native
    ports:
      - "8000:8080"
    environment:
      QUARKUS_DATASOURCE_JDBC_URL: "jdbc:sqlite:/data/users.db?journal_mode=WAL&busy_timeout=5000"
    volumes:
      - ./data:/data
    restart: unless-stopped
